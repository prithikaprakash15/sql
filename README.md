# sql
📘 A well-structured collection of SQL notes covering core concepts, queries, joins, subqueries, constraints, and interview-oriented examples. Useful for beginners and placement preparation.

## 📌 Topics Covered

- Introduction to SQL & Databases  
- SQL Data Types  
- `SELECT`, `WHERE`, `ORDER BY`, `LIMIT`  
- Aggregate Functions (`COUNT`, `SUM`, `AVG`, `MIN`, `MAX`)  
- `GROUP BY` & `HAVING`  
- Joins  
  - INNER JOIN  
  - LEFT JOIN  
  - RIGHT JOIN  
  - SELF JOIN  
- Subqueries  
- Constraints  
  - PRIMARY KEY  
  - FOREIGN KEY  
  - UNIQUE  
  - NOT NULL  
- SQL Functions  
- Views & Indexes (Basics)  
- Common Interview Questions  
- Practice Queries (LeetCode-style)
